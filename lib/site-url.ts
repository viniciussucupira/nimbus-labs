/**
 * Where this site lives, in one place.
 *
 * The layout needs it for metadataBase, and the sitemap and robots files need
 * it to write absolute URLs. A search engine is given one address or none;
 * two files disagreeing about the address is how half a site goes unindexed.
 */
export const SITE_URL = "https://nimbuslabsai.com";
